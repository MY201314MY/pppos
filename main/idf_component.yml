dependencies:
  espressif/esp_modem:
    version: ^1.0.0
  espressif/esp_modem_usb_dte:
    rules:
    - if: idf_version >=4.4
    - if: target in [esp32s2, esp32s3]
    version: ^1.1.0
  idf:
    version: '>=4.1.0'
